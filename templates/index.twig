{% extends "base.twig" %}

{% block content %}
	<div class="container-fluid {{sidebar_class}}">
		<div class="content-wrapper">
			{% for post in posts %}
				{% include ['tease-'~post.post_type~'.twig','tease-' ~ post.category.slug ~ '.twig', 'tease.twig'] %}
			{% endfor %}
			{{function('proton_pagination')}}
		</div>
	</div>
{% endblock %}